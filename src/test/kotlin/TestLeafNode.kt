package com.riywo.ninja.bptree

import org.junit.jupiter.api.Test
import org.assertj.core.api.Assertions.*

class TestLeafNode {
    private val leafNode = LeafNode()

    @Test
    fun `insert 1,a`() {
        //leafNode.insert(Record(1, "a"))
    }
}